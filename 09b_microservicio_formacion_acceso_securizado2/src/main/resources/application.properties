spring.application.name=servicio-formacion
server.port=9001
remote.url=http://servicio-alumnos/escuela/alumnos

remote.user=${REMOTE_USER}
remote.password=${REMOTE_PASS}
eureka.client.service-url.defaultZone=http://${HOST_EUREKA}:8761/eureka
eureka.instance.hostname=${HOST_FORMACION}
eureka.instance.non-secure-port=${PORT_FORMACION}